
name: 'snowplow_event_splitting'
version: '0.0.1'
config-version: 2

profile: 'default'

model-paths: ["models"]
docs-paths: ["docs"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

vars:
  snowplow_event_splitting:
    snowplow__incremental_materialization: "snowplow_incremental"
